server:
  port: 8080

spring:
  application:
    name: auto-service-api
  jackson:
    default-property-inclusion: non_null

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /api-docs
